ignores:
  - "openspec/**"
  - "docs/openspec/**"
  # auto-generated at release time (scripts/release-full.sh -> bump-version's
  # changelog.sh) from commit subjects; angle-bracket text in a subject (e.g. a
  # <name> placeholder, #207) trips MD033/no-inline-html. It is a generated
  # artifact — hand-fixing gets overwritten on the next release.
  - "CHANGELOG.md"
