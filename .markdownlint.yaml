# markdownlint configuration for yibi-stack
# SKILL.md files use long table columns, code blocks, and bold-as-section-label
# These rules are relaxed to match the project's writing conventions.
default: true
MD013: false
MD024:
  siblings_only: true
MD036: false
