# Event Storming Handoff — {{change_name}}

> Generated by `/event-storming` skill.
> Input for `spectra-amplifier` Step 0.

---

## Domain Events

過去式事件（Past tense），每個事件代表系統中一個重要的事情發生了。

1. `EVENT_NAME`: DESCRIPTION
2. `EVENT_NAME`: DESCRIPTION
3. `EVENT_NAME`: DESCRIPTION

---

## Bounded Contexts

相關領域事件和業務規則的邊界。名稱用英文 kebab-case。

### `CONTEXT_NAME`

**Responsibility**: 一句話說明此 context 負責什麼

**Domain Events in this context**:

- `EVENT_NAME`
- `EVENT_NAME`

**External dependencies**:

- Upstream: context or system that produces data
- Downstream: context or system that consumes data

---

## Aggregate Roots

每個 Bounded Context 的一致性邊界實體。

### `AggregateName` (in `CONTEXT_NAME`)

**Entities under this aggregate**:

- `EntityName` — role
- `EntityName` — role

**Key invariants** (business rules that must always hold):

- INVARIANT

---

## Notes for Amplifier

amplifier Step 4 假設與約束的前置輸入。

- **Critical assumptions to verify in Step 4**:
  - ASSUMPTION
- **Hard constraints identified during Event Storming**:
  - CONSTRAINT
- **Out of Scope items surfaced**:
  - ITEM — reason: WHY
