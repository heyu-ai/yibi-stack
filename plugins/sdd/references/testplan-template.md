# {{change_name}} — Test Plan

> Generated by `spectra-amplifier` Step 2.
> Source: `openspec/changes/{{change_name}}/specs/`

trace: enforced

<!-- trace: enforced opts this plan into check_testplan_trace.py's FAIL ratchet: once tasks.md is
     fully checked (or under --strict), every auto TC needs a test whose docstring has `tc: <TC-ID>`,
     and every Manual Verification item must be checked. Remove it only for a non-Python project. -->

---

## Test Seams

The public boundaries this change is tested at. Confirmed by a human during propose review; no TC
may target a seam missing from this table (amplifier-verify Check 4). Mocks only at external
boundaries — a seam that needs this repo's own modules mocked is the wrong seam.

| Seam | Public interface | Why here |
|------|------------------|----------|
| `login-api` | `POST /api/v1/auth/login` | the contract the mobile client depends on |

---

## Coverage Analysis

| Scenario slug | Covered | Technique | TC-ID(s) | Notes |
|--------------|---------|-----------|---------|-------|
| `<slug>` | ✓ | BVA | LOGIN-VL-001 | — |
| `<slug>` | △ partial | DT | LOGIN-DT-001 | missing error path |
| `<slug>` | ✗ missing | — | — | no test yet |

Legend: ✓ covered · △ partial · ✗ missing

---

## TC Table

| TC-ID | Kind | Seam | Test Purpose | Technique | Risk | Precondition | Steps | Test Data | Expected Result |
|-------|------|------|-------------|-----------|------|-------------|-------|-----------|----------------|
| LOGIN-VL-001 | auto | `login-api` | Verify empty password rejected | BVA | High | User not logged in | 1. Submit empty password | password='' | Return 422 Unprocessable |

`Kind` is `auto` for every row here: each needs a test with `tc: <TC-ID>` in its docstring. A check
no test can automate does not go in this table — list it under Manual Verification instead.

---

## Manual Verification

Checks a human runs during pr-cycle-deep Step 8; each is ticked after the human confirms it.

- [ ] MV-001 Reviewer sees the demoted finding with its reason in `final.md` (scenario: `<slug>`)

---

## Missing Coverage

List scenarios not yet traced to any test:

- `<cap>#<slug>` — reason / plan

---

## Redundant TCs

List test IDs that cover the same scenario as another without adding new value:

- `TC-ID` duplicates coverage of `TC-ID2`

---

## Traceability Matrix

| US | Scenario slug | TC-ID | pytest docstring |
|----|--------------|-------|-----------------|
| US-001 | `<slug>` | LOGIN-VL-001 | `spec: <cap>#<slug>` + `tc: LOGIN-VL-001` |
